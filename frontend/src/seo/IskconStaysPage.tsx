import React from "react";
import { SeoLandingPage } from "./SeoLandingPage";
import { SEO_LANDING_CONFIGS } from "./seoLandingConfigs";

export const IskconStaysPage: React.FC = () => {
  return <SeoLandingPage config={SEO_LANDING_CONFIGS.iskcon} />;
};

export default IskconStaysPage;
