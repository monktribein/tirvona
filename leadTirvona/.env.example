# Copy to .env and adjust for your environment.

# Base URL of the Tirvona backend that serves the Lead Collection module.
# Host only — the app appends /api/lead-collection.
VITE_LEAD_API_URL="http://localhost:5000"
